<h2>My profile</h2>

<%if @employees%>
	<div class="table_block">
		<table>
			<tr>
				<th>Name</th>
				<th>Address</th>
				<th>Contact</th>
				<th>Company Join Date</th>
				<th>Role</th>
				<th>Email</th>
			</tr>

			<tr>
				<td><%= @employees.name.capitalize %></td>
				<td><%= @employees.address.capitalize%></td>
				<td><%= @employees.contact%></td>
				<td><%= @employees.start_date%></td>
				<td><%= @employees.role.capitalize %></td>
				<td><%= @employees.email%></td>
				<td><%= link_to "Update information", edit_employee_path(@employees)%></td>
			</tr>
		</table>
	</div>
<% end %>
<%= link_to "Back to dashboard", dashboard_path%>
